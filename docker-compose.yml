services:
  app:
    build: .
    container_name: gorzdrav_bot
    restart: unless-stopped
    ports:
      # Наружу порт не выставляем — доступ только через nginx
      - "127.0.0.1:${PORT:-3003}:${PORT:-3003}"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
